/**
 * This file defines unique names for each service. This is used by the fluxible service
 * proxy plugin to resolve the service implementations on the client or server, depending
 * on where the code is executing.
 */

module.exports = {
  ArticleService: 'articleService'
}
